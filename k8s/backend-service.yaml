apiVersion: v1
kind: Service
metadata:
  name: policycollector-backend
  namespace: policycollector
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
